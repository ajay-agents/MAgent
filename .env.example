OPENAI_API_KEY=sk-...          # GPT-4o-mini (primary). Use sk-test-placeholder to activate mock mode.
GEMINI_API_KEY=                # Gemini 2.0 Flash (fallback, optional)
JWT_SECRET=change-me-in-prod   # Any long random string
FERNET_KEY=                    # Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
